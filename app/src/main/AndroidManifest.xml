<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.usjt.arqdesis.clienteprest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="br.usjt.arqdesis.clienteprest.controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="br.usjt.arqdesis.clienteprest.controller.ListaClientesActivity" />
        <activity android:name="br.usjt.arqdesis.clienteprest.controller.DetalheClienteActivity"></activity>

    </application>

</manifest>
